package proj_sp5;
class Driver {
	public static void main(final String[] args) {
		Unit test;
		Structure test2;
		// Get unit from user, write it to the file
		// "temp.sun", then repeat as long as the user wants
		// (but at least once). Then close the file. Do the
		// same thing with structures, writing them to
		// "temp.sst"
	}
}
